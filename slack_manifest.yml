# Used for Slack OAuth

display_information:
  name: Surveyor
oauth_config:
  redirect_urls:
    - http://localhost:3000/auth/slack/callback
settings:
  org_deploy_enabled: false
  socket_mode_enabled: false
  token_rotation_enabled: false
